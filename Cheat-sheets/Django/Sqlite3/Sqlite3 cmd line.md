- In gitpod workspace may have to run 
``` bash
 $ sudo apt update
```

- then 
``` bash
$ sudo apt install sqlite3
```
- for prompt to run SQL commands
``` bash
$ sqlite3
sqlite> SELECT * FROM ....
```
- 